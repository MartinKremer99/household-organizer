# Public. Safe for the browser. Copy to .env.local.
# Dashboard → Connect, or Settings → API Keys (publishable key, not the legacy anon JWT).
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
